<%= form_with(model: @compartment_carriage, local: true) do |form| %>

  <%= render 'common/errors', resource: @compartment_carriage %>

  <div class="field">
    <%= form.label :upper_places %>
    <%= form.text_field :upper_places %>
  </div>


  <div class="field">
    <%= form.label :bottom_places %>
    <%= form.text_field :bottom_places %>
  </div>

  <div class="field">
    <%= form.label :train %>
    <%= form.collection_select :train_id, Train.all, :id, :number %>
  </div>

  <div class="actions">
    <%= form.submit "#{ @compartment_carriage.new_record? ? 'Создать' : 'Изменить' }" %>
  </div>
<% end %>
